<%= form_for(lifestyle) do |f| %>
  <% if lifestyle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lifestyle.errors.count, "error") %> prohibited this lifestyle from being saved:</h2>

      <ul>
      <% lifestyle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
